# Changelog

## 0.1.0 (2026-03-15)

- Initial release
- `parallel()` for running callables in threads with ordered results
- `parallel_map()` for parallel iteration with worker control
- `aparallel()` for async coroutine concurrency
- `ParallelError` with `.errors` and `.results` for partial failure inspection
